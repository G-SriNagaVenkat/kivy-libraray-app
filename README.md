Get all books

Get a single book by ID

Add a new book (POST)

Uses a simple in-memory array (no database required)

📁 Project Structure
project/
│── server.js
│── books.js
│── README.md

▶️ Run the Project
 install all dependencies 
 --pip install kivy[Full]
 
